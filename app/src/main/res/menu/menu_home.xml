<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="swap.irfanullah.com.swap.HomeActivity">


    <item android:id="@+id/profile" android:title="Profile"
        app:showAsAction="never"
        />
    <item android:id="@+id/settings" android:title="Settings"
        app:showAsAction="never"
        />

    <item android:id="@+id/invite" android:title="Invite"
        app:showAsAction="never"
        />
    <item android:id="@+id/logout" android:title="Logout"
        app:showAsAction="never"
        />
    <item android:id="@+id/notification_icon"
       app:actionLayout="@layout/feed_update_count"
        android:icon="@drawable/ic_notifications_color"
        android:title="Notifications"
        app:showAsAction="always" />
</menu>
